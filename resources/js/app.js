import Vue from 'vue'
require('./bootstrap');

// Vue.component('back-view', require('./components/back/back-view').default);

// import Adminlte from 'admin-lte'
// Vue.use(Adminlte)

const app = new Vue({
    el: '#app',
});
